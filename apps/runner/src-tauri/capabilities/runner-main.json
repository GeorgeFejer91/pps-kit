{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "runner-main",
  "description": "Minimum core capability for the bundled PPS runner window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
